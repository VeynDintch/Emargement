{% extends 'base.html.twig' %}

{% block title %}SalleClasse{% endblock %}

{% block body %}
    <h1>SalleClasse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle_classe.id }}</td>
            </tr>
            <tr>
                <th>NomSalle</th>
                <td>{{ salle_classe.nomSalle }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ salle_classe.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_classe_index') }}">back to list</a>

    <a href="{{ path('app_salle_classe_edit', {'id': salle_classe.id}) }}">edit</a>

    {{ include('salle_classe/_delete_form.html.twig') }}
{% endblock %}
