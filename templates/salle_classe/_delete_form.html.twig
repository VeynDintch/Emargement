<form method="post" action="{{ path('app_salle_classe_delete', {'id': salle_classe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_classe.id) }}">
    <button class="btn">Delete</button>
</form>
